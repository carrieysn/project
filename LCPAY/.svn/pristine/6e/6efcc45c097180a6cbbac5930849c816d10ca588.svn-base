package com.cifpay.lc.bankadapter.open;

import org.springframework.stereotype.Component;

import com.cifpay.lc.bankadapter.api.input.AbsTradeParam;
import com.cifpay.lc.bankadapter.api.output.bank.GeneralTradeResult;
import com.cifpay.lc.bankadapter.api.output.bank.IBankTradeService;

@Component("bankTradeService2")
public class BankTradeService2 implements IBankTradeService {

	public GeneralTradeResult doTrade(AbsTradeParam param) {
		GeneralTradeResult r = new GeneralTradeResult();
		r.setTradeResult("0");
		r.setResultDesc("交易成功!");
		r.setRtnSerialNo("8431358");
		r.setRtnTradeDate("2016-04-18");
		r.setRtnCode("0000");
		r.setRtnMsg("成功");
		return r;
	}
}
